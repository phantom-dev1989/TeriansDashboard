/**
 * Created by stromero on 3/23/2015.
 */
'use strict'
describe('projectsRestService', function () {

    beforeEach(module('app'));

    it('/projects should return more than 0 projects', inject(function (projectsRestSvc) {

        // expect(projectsRestSvc.getCurrentProjects().length).toBeGreaterThan(0);
        //console.log(projectsRestSvc.getCurrentProjects());
    }))


});